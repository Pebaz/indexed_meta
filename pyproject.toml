[tool.poetry]
name = "indexed-meta"
version = "0.1.0"
description = ""
license = "MIT"
readme = "README.md"
packages = [{include = "indexed_meta"}]

[tool.poetry.dependencies]
python = "^3.5"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
